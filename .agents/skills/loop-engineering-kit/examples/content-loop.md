# Content Loop Example

## Goal
Create publishable content against a defined rubric.

## Flow
Define audience/objective → draft → independent critique → rewrite → score against rubric → fact/style check → publish or repair.

Use a bounded revision count. Do not endlessly rewrite for subjective marginal gains.
